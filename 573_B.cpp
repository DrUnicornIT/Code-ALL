#include <bits/stdc++.h>
#define LL long long 
using namespace std ;

int n ; 
int a[100010];

int main()
{
	ios_base::sync_with_stdio(false);
	cin.tie(0) ; cout.tie(0);

	freopen("main.inp","r",stdin);
	freopen("main.out","w",stdout);

	cin >> n ;
	for(int i = 1 ; i <= n ; i ++)
	{
		cin >> a[i];
	}
	a[0] = 0;
	a[n+1] =
	return 0;
}
